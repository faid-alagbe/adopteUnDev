<form method="post" action="{{ path('app_profils_dev_delete', {'id': profils_dev.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ profils_dev.id) }}">
    <button class="btn">Delete</button>
</form>
